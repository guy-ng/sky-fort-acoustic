---
phase: 22
plan: 05
type: execute
wave: 3  # scheduled in wave 3 (not wave 1) so the human reviewer is already engaged with the phase when the decision checkpoint hits, not before window-contract work (plans 02-04) has visible progress — avoids a cold checkpoint interruption
depends_on: [22-01]
files_modified:
  - scripts/investigate_kaggle_drone_dataset.py
  - .planning/phases/22-efficientat-v8-retrain-with-fixed-train-serve-window-contrac/KAGGLE_DATASET_INVESTIGATION.md
autonomous: false
requirements:
  - REQ-22-D1
must_haves:
  truths:
    - "The Kaggle notebook + DroneAudioDataset have been technically investigated (license, SR, labels, size)"
    - "A documented decision exists: INGEST or REJECT, with criteria cited"
    - "If ingest: a manifest of how to integrate into the v8 training set exists"
    - "If reject: the reason is in KAGGLE_DATASET_INVESTIGATION.md so we don't re-investigate"
  artifacts:
    - path: scripts/investigate_kaggle_drone_dataset.py
      provides: "fetches dataset metadata, probes sample rate + channels + license, prints summary"
    - path: .planning/phases/22-efficientat-v8-retrain-with-fixed-train-serve-window-contrac/KAGGLE_DATASET_INVESTIGATION.md
      provides: "investigation findings + ingest/reject decision"
  key_links:
    - from: KAGGLE_DATASET_INVESTIGATION.md
      to: scripts/investigate_kaggle_drone_dataset.py
      via: "report references the probe output"
      pattern: "investigate_kaggle_drone_dataset"
---

<objective>
User (mid-workflow) explicitly asked us to investigate an additional dataset: the Kaggle notebook https://www.kaggle.com/code/yehiellevi/audio-drone-sound-detection and its likely underlying source https://github.com/saraalemadi/DroneAudioDataset. Produce a technical investigation (autonomous) and then a human-in-the-loop ingest/reject decision (checkpoint) so license review gets a human eyeball.

Purpose: Honor the user's mid-workflow instruction. Do NOT silently drop this. Either integrate the data or document why not.
Output: A probe script + an investigation report with a decision.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-efficientat-v8-retrain-with-fixed-train-serve-window-contrac/CONTEXT.md
@.planning/phases/22-efficientat-v8-retrain-with-fixed-train-serve-window-contrac/22-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Technical investigation — fetch DroneAudioDataset metadata and probe audio properties</name>
  <files>
    scripts/investigate_kaggle_drone_dataset.py,
    .planning/phases/22-efficientat-v8-retrain-with-fixed-train-serve-window-contrac/KAGGLE_DATASET_INVESTIGATION.md
  </files>
  <read_first>
    .planning/phases/22-efficientat-v8-retrain-with-fixed-train-serve-window-contrac/CONTEXT.md
  </read_first>
  <action>
Produce a technical investigation of the Kaggle notebook + DroneAudioDataset. Do NOT download the full dataset (could be GB-scale) — just probe metadata.

**Step 1:** Create `scripts/investigate_kaggle_drone_dataset.py` that:
1. Uses `urllib` or `requests` (already in requirements) to GET https://raw.githubusercontent.com/saraalemadi/DroneAudioDataset/master/README.md and https://api.github.com/repos/saraalemadi/DroneAudioDataset
2. Prints: repo description, license (from GitHub API), size (approximate from the API), and the README content (first 200 lines)
3. If a `LICENSE` file path is known, fetches its raw content
4. If the repo has a manageable number of audio files (< 50 files total), offers to clone a shallow copy via `git clone --depth 1 --filter=blob:none` to a tmp dir and probe sample rates via `soundfile.info` for a random 5-file sample. If > 50 files, abort and print "too large — manual review required".
5. Prints a decision table template:
   ```
   DECISION CRITERIA (fill in):
   License compatible with project use?      [ ]
   Sample rate 16 kHz mono WAV?              [ ]
   Labels match drone / background schema?   [ ]
   Overlap with DADS or field recordings?    [ ]
   Total size < 5 GB?                        [ ]
   ```

**Step 2:** Run the investigation script via `python scripts/investigate_kaggle_drone_dataset.py > /tmp/kaggle_probe.txt 2>&1` (or capture to a variable), analyze the output, and write `.planning/phases/22-efficientat-v8-retrain-with-fixed-train-serve-window-contrac/KAGGLE_DATASET_INVESTIGATION.md`:

```markdown
# Kaggle Drone Dataset — Investigation (Phase 22)

**Requested by:** user mid-workflow, 2026-04-08
**Source:** https://www.kaggle.com/code/yehiellevi/audio-drone-sound-detection
**Likely upstream:** https://github.com/saraalemadi/DroneAudioDataset

## Technical Findings

| Field | Value | Source |
|---|---|---|
| License | <e.g. MIT / CC-BY-4.0 / unspecified> | GitHub API / LICENSE file |
| Size | <bytes / files> | GitHub API |
| Splits | UNAUGMENTED, AUGMENTED | README |
| Format | <WAV / FLAC / mp3> | probe |
| Sample rate | <probe result> | soundfile.info |
| Channels | <mono / stereo> | soundfile.info |
| Labels | <drone / noise / bee / unknown> | directory structure / README |
| Duration per clip | <range> | probe |
| Relationship to Kaggle notebook | <does the notebook reference this repo? any license chain gotchas?> | notebook markdown |

## Overlap Analysis

- DADS corpus: <yes/no overlap — same source recordings?>
- 2026-04-08 field recordings: <obviously no — different dates, different array>
- Risk of double-counting: <low/med/high>

## Decision Criteria (for human review in Task 2)

| Criterion | Met? | Notes |
|---|---|---|
| License compatible with project use | ☐ | |
| Sample rate can resample to 32 kHz without loss | ☐ | |
| Labels map to drone/not-drone cleanly | ☐ | |
| Not a subset of DADS (no double-counting) | ☐ | |
| Total size manageable (< 5 GB after subset) | ☐ | |
| Recording conditions defensible (not in-cabin / bee-mixed) | ☐ | |

## Preliminary Recommendation

<INGEST / REJECT / DEFER> because <reasons>

## Open Items for Human Review

- License legal review
- Operational fit — does the recording environment match UMA-16 outdoor operational use?
- Preferred split: ingest as additional training data, as additional BG negatives, or as a supplementary eval set?
```

**IMPORTANT:** This is the autonomous investigation ONLY. The ingest/reject decision is Task 2 (checkpoint) because license review needs a human eyeball.

If the GitHub API or raw URL fetching fails (network issues), document the failure and move Task 2 to "manual investigation required" — still produce the report skeleton.
  </action>
  <verify>
    <automated>test -f scripts/investigate_kaggle_drone_dataset.py && test -f .planning/phases/22-efficientat-v8-retrain-with-fixed-train-serve-window-contrac/KAGGLE_DATASET_INVESTIGATION.md && grep -q "Technical Findings" .planning/phases/22-efficientat-v8-retrain-with-fixed-train-serve-window-contrac/KAGGLE_DATASET_INVESTIGATION.md && grep -q "Preliminary Recommendation" .planning/phases/22-efficientat-v8-retrain-with-fixed-train-serve-window-contrac/KAGGLE_DATASET_INVESTIGATION.md</automated>
  </verify>
  <done>
    Investigation script exists, runs (or gracefully reports network failure), and KAGGLE_DATASET_INVESTIGATION.md is populated with real probe values (not templated placeholders).
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Human decision — INGEST, REJECT, or DEFER the Kaggle DroneAudioDataset</name>
  <read_first>
    .planning/phases/22-efficientat-v8-retrain-with-fixed-train-serve-window-contrac/KAGGLE_DATASET_INVESTIGATION.md
  </read_first>
  <action>Present the investigation report to the user, wait for a decision, and record the selection in KAGGLE_DATASET_INVESTIGATION.md under a new Final Decision section. If INGEST or INGEST-AS-SUPPLEMENTARY-EVAL, surface follow-up tasks for the next plan or a new phase. If REJECT or DEFER, ensure the reason is specific enough that we do not re-investigate.</action>
  <decision>Should the DroneAudioDataset be ingested into the v8 training set, rejected, or deferred to a future phase?</decision>
  <context>
User asked mid-workflow to investigate this dataset. Technical probe in Task 1 produced KAGGLE_DATASET_INVESTIGATION.md. License review and operational-fit judgment need human eyeballs — this is not an autonomous call.

Key inputs to the decision:
1. License compatibility (from investigation report)
2. Operational fit — does the recording environment resemble UMA-16 outdoor ops?
3. Overlap with DADS (risk of double-counting)
4. Timeline — Phase 22 is already ~9 plans; adding dataset ingest could blow the L4 training budget
  </context>
  <options>
    <option id="ingest">
      <name>INGEST — integrate into v8 training</name>
      <pros>More training data, potentially better drone classifier generalization</pros>
      <cons>Adds dataset ingestion work (resample to 32kHz, label normalization, manifest update, class balance re-check). Delays v8 training run by ~1 day. Legal/license review required.</cons>
    </option>
    <option id="ingest-as-supplementary-eval">
      <name>INGEST AS SUPPLEMENTARY EVAL SET (not training)</name>
      <pros>Third-party eval set surfaces generalization gaps without touching training data distribution. Lower risk. No class-balance impact.</pros>
      <cons>Still requires license review. D-27 gate stays on UMA-16 hold-out — this becomes a secondary signal only.</cons>
    </option>
    <option id="reject">
      <name>REJECT — document reason and move on</name>
      <pros>Phase 22 stays focused on the v7-regression fix; no delay. Clean scope.</pros>
      <cons>User will want a clear "why not" reason in the summary.</cons>
    </option>
    <option id="defer">
      <name>DEFER to a future phase (e.g., Phase 23)</name>
      <pros>Phase 22 ships the window-contract fix; Kaggle ingest gets proper breathing room in its own phase with clean scope.</pros>
      <cons>Requires a new roadmap entry and explicit user sign-off on deferral.</cons>
    </option>
  </options>
  <resume-signal>Select: ingest, ingest-as-supplementary-eval, reject, or defer — and reply with a one-line rationale that will be pasted into KAGGLE_DATASET_INVESTIGATION.md's Final Decision section.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| third-party dataset → training corpus | license + content review before ingestion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering | training data poisoning (third-party labels) | mitigate | Human decision checkpoint before ingest; technical probe documents SR/labels/license |
| T-22-legal | Legal | license incompatibility | mitigate | Human review of license is the decision gate |
</threat_model>

<verification>
KAGGLE_DATASET_INVESTIGATION.md exists with populated Technical Findings and a human-made Final Decision.
</verification>

<success_criteria>
- Technical investigation committed
- Human decision recorded in the report
- If REJECT: reason is clear and specific (so we don't re-investigate)
- If INGEST: follow-up tasks identified for a later plan or a new phase
</success_criteria>

<output>
After completion, create `.planning/phases/22-efficientat-v8-retrain-with-fixed-train-serve-window-contrac/22-05-SUMMARY.md`
</output>
