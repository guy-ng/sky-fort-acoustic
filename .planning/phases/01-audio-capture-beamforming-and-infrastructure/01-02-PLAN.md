---
phase: 01-audio-capture-beamforming-and-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/acoustic/beamforming/__init__.py
  - src/acoustic/beamforming/geometry.py
  - src/acoustic/beamforming/gcc_phat.py
  - src/acoustic/beamforming/srp_phat.py
  - src/acoustic/beamforming/peak.py
  - tests/unit/test_geometry.py
  - tests/unit/test_gcc_phat.py
  - tests/unit/test_srp_phat.py
  - tests/unit/test_peak.py
autonomous: true
requirements:
  - BF-01
  - BF-02
  - BF-03
  - BF-04

must_haves:
  truths:
    - "SRP-PHAT produces a 2D spatial map (azimuth x elevation) from 16-channel audio"
    - "Peak detection returns azimuth and elevation in degrees for the strongest source"
    - "Beamforming frequency band is configurable (default 100-2000 Hz)"
    - "Noise threshold filters weak peaks using percentile-based calibration with configurable margin"
    - "Mic geometry matches POC: 4x4 URA, 42mm spacing, channel mapping from mechanical drawing"
  artifacts:
    - path: "src/acoustic/beamforming/srp_phat.py"
      provides: "2D SRP-PHAT beamforming engine"
      contains: "def srp_phat_2d"
    - path: "src/acoustic/beamforming/gcc_phat.py"
      provides: "GCC-PHAT cross-correlation"
      contains: "def gcc_phat_from_fft"
    - path: "src/acoustic/beamforming/geometry.py"
      provides: "Mic positions and 2D steering vectors"
      contains: "def build_steering_vectors_2d"
    - path: "src/acoustic/beamforming/peak.py"
      provides: "Peak detection with adaptive noise threshold"
      contains: "def detect_peak_with_threshold"
    - path: "tests/unit/test_srp_phat.py"
      provides: "SRP-PHAT validation with synthetic audio"
      contains: "def test_"
  key_links:
    - from: "src/acoustic/beamforming/srp_phat.py"
      to: "src/acoustic/beamforming/gcc_phat.py"
      via: "gcc_phat_from_fft called per mic pair"
      pattern: "gcc_phat_from_fft"
    - from: "src/acoustic/beamforming/srp_phat.py"
      to: "src/acoustic/beamforming/geometry.py"
      via: "build_steering_vectors_2d for direction grid"
      pattern: "build_steering_vectors_2d"
    - from: "src/acoustic/beamforming/peak.py"
      to: "numpy"
      via: "np.percentile for adaptive threshold"
      pattern: "np\\.percentile"
---

<objective>
Implement the SRP-PHAT beamforming engine with 2D spatial map output, GCC-PHAT cross-correlation, mic geometry, and peak detection with adaptive noise threshold.

Purpose: This is the core signal processing that converts raw 16-channel audio into a spatial power map showing sound source directions. It is the intelligence layer between audio capture (Plan 01) and the health/status reporting (Plan 03).
Output: Complete beamforming module that takes (n_mics, n_samples) audio and produces a 2D spatial map with peak azimuth/elevation detection.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-audio-capture-beamforming-and-infrastructure/01-CONTEXT.md
@.planning/phases/01-audio-capture-beamforming-and-infrastructure/01-RESEARCH.md
@POC-code/PT520/PTZ/radar_gui_all_mics_fast_drone.py

<interfaces>
<!-- From src/acoustic/config.py (created in Plan 01, but no import dependency needed — beamforming functions take raw parameters) -->
<!-- Beamforming module is pure computation: takes arrays and scalars, returns arrays. No coupling to config. -->

<!-- From POC radar_gui_all_mics_fast_drone.py — the algorithms to port: -->
```python
# POC constants (lines 11-22)
FS = 48000; C = 343.0; SPACING = 0.042
DRONE_FMIN = 100.0; DRONE_FMAX = 2000.0

# POC functions to port and extend:
def build_mic_positions() -> np.ndarray:  # (3, 16) — lines 26-72
def prepare_fft(signals, fs) -> tuple:     # lines 92-118
def gcc_phat_from_fft(Xm, Xn, nfft, max_shift, band_mask) -> np.ndarray:  # lines 121-134
def srp_phat_1d_fast(signals, mic_positions, fs, c, angle_grid_deg) -> np.ndarray:  # lines 137-181
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mic geometry, GCC-PHAT, and 2D steering vectors</name>
  <files>
    src/acoustic/beamforming/__init__.py,
    src/acoustic/beamforming/geometry.py,
    src/acoustic/beamforming/gcc_phat.py,
    tests/unit/test_geometry.py,
    tests/unit/test_gcc_phat.py
  </files>
  <read_first>
    POC-code/PT520/PTZ/radar_gui_all_mics_fast_drone.py (build_mic_positions lines 26-72 for channel mapping, prepare_fft lines 92-118 for FFT setup, gcc_phat_from_fft lines 121-134),
    .planning/phases/01-audio-capture-beamforming-and-infrastructure/01-RESEARCH.md (Pattern 2: 2D SRP-PHAT Extension — build_steering_vectors_2d function, Mic Position Geometry code example),
    .planning/phases/01-audio-capture-beamforming-and-infrastructure/01-CONTEXT.md (D-06 spatial map format, D-07 peak output format)
  </read_first>
  <behavior>
    Geometry tests:
    - test_mic_positions_shape: build_mic_positions() returns shape (3, 16)
    - test_mic_positions_planar: All z-coordinates are 0.0 (planar array)
    - test_mic_positions_spacing: Max x distance = 3 * 0.042 = 0.126m, max y distance = 3 * 0.042 = 0.126m
    - test_mic_positions_channel_mapping: Channel 0 (MIC1) is at row=3, col=1 per POC mapping, so x=xs[1]=-0.5*0.042, y=ys[3]=-1.5*0.042
    - test_steering_vectors_2d_shape: build_steering_vectors_2d(np.arange(-90,91,3), np.arange(-45,46,3)) returns shape (61*31, 3)
    - test_steering_vectors_2d_unit: All vectors have norm 1.0 (unit vectors)
    - test_steering_vectors_broadside: At az=0, el=0, direction vector is (0, 1, 0) — pointing along y-axis (front)

    GCC-PHAT tests:
    - test_prepare_fft_shape: With (16, 7200) input at fs=48000, returns X shape (16, nfft//2+1) where nfft is next power of 2 >= 2*7200
    - test_prepare_fft_band_mask: band_mask selects only frequencies between 100-2000 Hz
    - test_gcc_phat_zero_delay: Two identical signals produce GCC-PHAT peak at shift=0
    - test_gcc_phat_known_delay: Signal delayed by 1 sample produces peak at shift=1
  </behavior>
  <action>
    **src/acoustic/beamforming/geometry.py:**
    - `SPACING = 0.042` (meters, 42mm between adjacent mics)
    - `NUM_CHANNELS = 16`
    - `def build_mic_positions() -> np.ndarray`: Port EXACTLY from POC lines 26-72. mic_rc mapping: {8:(0,0), 7:(0,1), 10:(0,2), 9:(0,3), 6:(1,0), 5:(1,1), 12:(1,2), 11:(1,3), 4:(2,0), 3:(2,1), 14:(2,2), 13:(2,3), 2:(3,0), 1:(3,1), 16:(3,2), 15:(3,3)}. xs = [-1.5, -0.5, 0.5, 1.5] * SPACING. ys = [+1.5, +0.5, -0.5, -1.5] * SPACING. Returns (3, 16) array.
    - `def build_steering_vectors_2d(az_grid_deg: np.ndarray, el_grid_deg: np.ndarray) -> np.ndarray`: Per RESEARCH Pattern 2. Create meshgrid of (az, el) in radians, compute unit vectors in Cartesian: x=sin(az)*cos(el), y=cos(az)*cos(el), z=sin(el). Return (n_az * n_el, 3) array.

    **src/acoustic/beamforming/gcc_phat.py:**
    - `def prepare_fft(signals: np.ndarray, fs: int, fmin: float = 100.0, fmax: float = 2000.0) -> tuple[np.ndarray, int, int, np.ndarray]`: Port from POC lines 92-118. Remove DC per channel. nfft = next power of 2 >= 2*n_samples. Return (X, nfft, max_shift, band_mask). band_mask = (freqs >= fmin) & (freqs <= fmax).
    - `def gcc_phat_from_fft(Xm: np.ndarray, Xn: np.ndarray, nfft: int, max_shift: int, band_mask: np.ndarray) -> np.ndarray`: Port EXACTLY from POC lines 121-134. Apply band_mask to both spectra, compute cross-spectrum R = Xm * conj(Xn), normalize by |R| + 1e-12, IFFT, rearrange to centered correlation.

    **src/acoustic/beamforming/__init__.py:** Export build_mic_positions, build_steering_vectors_2d, prepare_fft, gcc_phat_from_fft.

    **tests/unit/test_geometry.py, test_gcc_phat.py:** Implement tests per behavior block. Use numpy.testing.assert_allclose for floating point comparisons.
  </action>
  <verify>
    <automated>cd /Users/guyelisha/Projects/sky-fort-acoustic && pytest tests/unit/test_geometry.py tests/unit/test_gcc_phat.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - src/acoustic/beamforming/geometry.py contains `SPACING = 0.042` and `def build_mic_positions` and `8: (0, 0), 7: (0, 1)`
    - src/acoustic/beamforming/geometry.py contains `def build_steering_vectors_2d(` with `np.meshgrid`
    - src/acoustic/beamforming/gcc_phat.py contains `def prepare_fft(` and `def gcc_phat_from_fft(`
    - src/acoustic/beamforming/gcc_phat.py contains `np.fft.rfft` and `band_mask` and `1e-12`
    - tests/unit/test_geometry.py contains `test_mic_positions_shape` and `test_steering_vectors_2d_unit`
    - tests/unit/test_gcc_phat.py contains `test_gcc_phat_zero_delay` and `test_prepare_fft_band_mask`
    - pytest tests/unit/test_geometry.py tests/unit/test_gcc_phat.py exits 0
  </acceptance_criteria>
  <done>
    Mic geometry ported from POC with verified channel mapping. 2D steering vectors generate unit direction vectors for arbitrary azimuth/elevation grids. GCC-PHAT produces band-limited cross-correlations matching POC algorithm.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 2D SRP-PHAT engine and peak detection with noise threshold</name>
  <files>
    src/acoustic/beamforming/srp_phat.py,
    src/acoustic/beamforming/peak.py,
    tests/unit/test_srp_phat.py,
    tests/unit/test_peak.py
  </files>
  <read_first>
    src/acoustic/beamforming/geometry.py (build_mic_positions, build_steering_vectors_2d — signatures and return shapes),
    src/acoustic/beamforming/gcc_phat.py (prepare_fft, gcc_phat_from_fft — signatures and return shapes),
    POC-code/PT520/PTZ/radar_gui_all_mics_fast_drone.py (srp_phat_1d_fast lines 137-181 — the algorithm to extend to 2D),
    .planning/phases/01-audio-capture-beamforming-and-infrastructure/01-RESEARCH.md (Pattern 2: srp_phat_2d function, Percentile-Based Noise Threshold code example, Pitfall 3: performance budget),
    .planning/phases/01-audio-capture-beamforming-and-infrastructure/01-CONTEXT.md (D-06 map format, D-07 peak output, D-08 noise threshold)
  </read_first>
  <behavior>
    SRP-PHAT tests:
    - test_srp_phat_2d_output_shape: With az_grid=np.arange(-90,91,3) (61 points) and el_grid=np.arange(-45,46,3) (31 points), output shape is (61, 31)
    - test_srp_phat_2d_detects_broadside: Synthetic source at az=0, el=0 produces peak within 5 degrees of (0, 0) in the spatial map
    - test_srp_phat_2d_detects_off_axis: Synthetic source at az=30, el=0 produces peak within 5 degrees of az=30
    - test_srp_phat_2d_freq_band: With fmin=500, fmax=1000 and source at 750Hz, peak is detected. With source at 100Hz (outside band), peak power is significantly lower
    - test_srp_phat_2d_no_negative_values: Output map has no negative values (SRP accumulates correlation values)

    Peak detection tests:
    - test_detect_peak_strong_source: Map with clear peak at known (az, el) returns PeakDetection with correct az_deg and el_deg within 1 degree
    - test_detect_peak_below_threshold: Uniform noise map (no peak above threshold) returns None
    - test_detect_peak_noise_threshold: With percentile=95 and margin=1.5, a peak at 2x the 95th percentile is detected; a peak at 1.1x is not
    - test_detect_peak_configurable_percentile: Changing percentile from 95 to 50 lowers the threshold, detecting weaker peaks
  </behavior>
  <action>
    **src/acoustic/beamforming/srp_phat.py** — Extend POC's srp_phat_1d_fast to 2D per RESEARCH Pattern 2:
    - `def srp_phat_2d(signals: np.ndarray, mic_positions: np.ndarray, fs: int, c: float, az_grid_deg: np.ndarray, el_grid_deg: np.ndarray, fmin: float = 100.0, fmax: float = 2000.0) -> np.ndarray`:
      1. signals shape: (n_mics, n_samples) — transposed from sounddevice's (samples, channels)
      2. Call build_steering_vectors_2d(az_grid_deg, el_grid_deg) to get (n_dirs, 3) direction vectors
      3. Call prepare_fft(signals, fs, fmin, fmax) to get X, nfft, max_shift, band_mask
      4. Initialize srp = np.zeros(n_dirs, dtype=np.float64)
      5. For each mic pair (m, n) in itertools.combinations(range(n_mics), 2):
         - cc = gcc_phat_from_fft(X[m], X[n], nfft, max_shift, band_mask)
         - delta_p = mic_positions[:, m] - mic_positions[:, n]
         - tdoa_pred = dirs @ delta_p / c
         - shift_pred = np.round(tdoa_pred * fs).astype(int)
         - shift_pred = np.clip(shift_pred, -max_shift + 1, max_shift - 1)
         - srp += cc[shift_pred + max_shift]
      6. Return srp.reshape(len(az_grid_deg), len(el_grid_deg))
    - Import from geometry: build_steering_vectors_2d
    - Import from gcc_phat: prepare_fft, gcc_phat_from_fft

    **src/acoustic/beamforming/peak.py** — Per D-07 and D-08:
    - `def detect_peak_with_threshold(srp_map: np.ndarray, az_grid_deg: np.ndarray, el_grid_deg: np.ndarray, percentile: float = 95.0, margin: float = 1.5) -> PeakDetection | None`:
      1. threshold = np.percentile(srp_map, percentile) * margin
      2. max_val = np.max(srp_map)
      3. If max_val < threshold: return None
      4. az_idx, el_idx = np.unravel_index(np.argmax(srp_map), srp_map.shape)
      5. Return PeakDetection(az_deg=float(az_grid_deg[az_idx]), el_deg=float(el_grid_deg[el_idx]), power=float(max_val), threshold=float(threshold))
    - Import PeakDetection from src/acoustic/types.py

    Update **src/acoustic/beamforming/__init__.py:** Add exports: srp_phat_2d, detect_peak_with_threshold.

    **tests/unit/test_srp_phat.py:** Use the generate_simulated_chunk function (from conftest.py synthetic_audio fixture or inline) to create test signals. Use coarse grid (3-degree steps) for faster tests.

    **tests/unit/test_peak.py:** Construct synthetic SRP maps with known peaks for deterministic testing.
  </action>
  <verify>
    <automated>cd /Users/guyelisha/Projects/sky-fort-acoustic && pytest tests/unit/test_srp_phat.py tests/unit/test_peak.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - src/acoustic/beamforming/srp_phat.py contains `def srp_phat_2d(` and `itertools.combinations` and `gcc_phat_from_fft` and `.reshape(len(az_grid_deg), len(el_grid_deg))`
    - src/acoustic/beamforming/peak.py contains `def detect_peak_with_threshold(` and `np.percentile` and `np.unravel_index` and `PeakDetection`
    - tests/unit/test_srp_phat.py contains `test_srp_phat_2d_output_shape` and `test_srp_phat_2d_detects_broadside` and `test_srp_phat_2d_freq_band`
    - tests/unit/test_peak.py contains `test_detect_peak_strong_source` and `test_detect_peak_below_threshold` and `test_noise_threshold`
    - pytest tests/unit/test_srp_phat.py tests/unit/test_peak.py exits 0
  </acceptance_criteria>
  <done>
    2D SRP-PHAT produces spatial maps from 16-channel audio with configurable frequency band (BF-01, BF-02). Peak detection returns azimuth/elevation in degrees (BF-03) with percentile-based noise threshold (BF-04). Validated with synthetic directional audio.
  </done>
</task>

</tasks>

<verification>
- All beamforming tests pass: `pytest tests/unit/test_geometry.py tests/unit/test_gcc_phat.py tests/unit/test_srp_phat.py tests/unit/test_peak.py -x -v`
- Synthetic source at az=30 produces SRP-PHAT peak near 30 degrees
- Noise-only input produces no detection (None from detect_peak_with_threshold)
- Frequency band filtering reduces out-of-band source detection power
</verification>

<success_criteria>
- srp_phat_2d returns correct-shaped 2D map for any azimuth/elevation grid
- Peak detection correctly identifies strongest source direction within 5 degrees
- Noise threshold rejects weak/absent sources
- Frequency band configuration demonstrably affects detection
- GCC-PHAT and geometry match POC algorithm
- All tests green: 15+ passing tests
</success_criteria>

<output>
After completion, create `.planning/phases/01-audio-capture-beamforming-and-infrastructure/01-02-SUMMARY.md`
</output>
