---
phase: 19-functional-beamforming-visualization
plan: 02
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - web/src/components/heatmap/HeatmapCanvas.tsx
autonomous: false
requirements: [VIZ-02]

must_haves:
  truths:
    - "Frontend maps backend [0,1] values directly to colormap without any additional transform"
    - "Heatmap renders sharper peaks because backend functional beamforming now owns contrast"
  artifacts:
    - path: "web/src/components/heatmap/HeatmapCanvas.tsx"
      provides: "Direct value-to-LUT mapping without squaring"
      contains: "Math.round(v * 255) * 3"
  key_links:
    - from: "web/src/components/heatmap/HeatmapCanvas.tsx"
      to: "/ws/heatmap"
      via: "WebSocket binary float32 frames"
      pattern: "Math\\.round\\(v \\* 255\\)"
---

<objective>
Remove frontend squared normalization (`v * v`) from HeatmapCanvas so the heatmap directly maps backend-normalized [0,1] functional beamforming values to the jet colormap LUT.

Purpose: Per D-08, the backend now owns all contrast/normalization through functional beamforming. The frontend squaring is redundant and would double-compress the dynamic range. Removing it ensures the heatmap faithfully renders the sidelobe-suppressed map.

Output: HeatmapCanvas renders backend values directly; visual verification of sharper peaks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-functional-beamforming-visualization/19-CONTEXT.md
@.planning/phases/19-functional-beamforming-visualization/19-UI-SPEC.md

@web/src/components/heatmap/HeatmapCanvas.tsx

<interfaces>
From web/src/components/heatmap/HeatmapCanvas.tsx (lines 55-65):
```typescript
for (let i = 0; i < expectedLen; i++) {
  const v = floats[i]
  // Squared normalization for visual contrast (POC uses alpha**2)
  const normalized = v * v
  const lutIdx = Math.round(normalized * 255) * 3
  const pixIdx = i * 4
  pixels[pixIdx] = lut[lutIdx]
  pixels[pixIdx + 1] = lut[lutIdx + 1]
  pixels[pixIdx + 2] = lut[lutIdx + 2]
  pixels[pixIdx + 3] = 255
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove v*v squaring from HeatmapCanvas</name>
  <files>web/src/components/heatmap/HeatmapCanvas.tsx</files>
  <read_first>web/src/components/heatmap/HeatmapCanvas.tsx</read_first>
  <action>
In `web/src/components/heatmap/HeatmapCanvas.tsx`, replace the rendering loop body (approximately lines 56-59):

FROM:
```typescript
const v = floats[i]
// Squared normalization for visual contrast (POC uses alpha**2)
const normalized = v * v
const lutIdx = Math.round(normalized * 255) * 3
```

TO:
```typescript
const v = floats[i]
// Backend owns contrast via functional beamforming (D-08) -- direct LUT mapping
const lutIdx = Math.round(v * 255) * 3
```

This removes the `v * v` squaring and the intermediate `normalized` variable. The comment explains why no frontend transform is needed (per D-08).
  </action>
  <verify>
    <automated>cd /Users/guyelisha/Projects/sky-fort-acoustic/web && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - HeatmapCanvas.tsx does NOT contain `v * v` or `normalized = v * v`
    - HeatmapCanvas.tsx contains `Math.round(v * 255) * 3` (direct mapping)
    - HeatmapCanvas.tsx contains comment referencing D-08 or "functional beamforming"
    - vitest run exits 0
  </acceptance_criteria>
  <done>Frontend renders backend functional beamforming output directly without additional contrast transform</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of functional beamforming heatmap</name>
  <files>web/src/components/heatmap/HeatmapCanvas.tsx</files>
  <action>
Human verifies that the heatmap displays sharper, sidelobe-suppressed peaks after functional beamforming changes.
  </action>
  <verify>
    <automated>echo "checkpoint:human-verify -- manual visual inspection required"</automated>
  </verify>
  <done>Human confirms heatmap shows distinct source peaks with clean dark background</done>
  <what-built>Functional beamforming visualization: backend applies nu=100 power-map exponent for sidelobe suppression, frontend renders directly without squaring. The heatmap should show sharper, more distinct source peaks with cleaner dark backgrounds compared to the previous smeared display.</what-built>
  <how-to-verify>
    1. Start the service: `docker compose up` or run locally
    2. Open the web UI heatmap page
    3. Play audio with a known source direction (or use recorded audio)
    4. Verify: peaks are sharp and distinct (not smeared blobs)
    5. Verify: background is clean dark blue (sidelobes suppressed)
    6. Optional: PATCH the nu parameter to see effect:
       `curl -X PATCH http://localhost:8000/api/settings -H "Content-Type: application/json" -d '{"bf_nu": 10}'`
       Heatmap should become more smeared with lower nu.
       Then restore: `curl -X PATCH http://localhost:8000/api/settings -H "Content-Type: application/json" -d '{"bf_nu": 100}'`
  </how-to-verify>
  <resume-signal>Type "approved" or describe visual issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebSocket /ws/heatmap -> browser | Data flows from backend to frontend (read-only, no threat) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-04 | Information Disclosure | HeatmapCanvas rendering | accept | Heatmap displays non-sensitive spatial power data. No PII or secrets. |
</threat_model>

<verification>
- `cd web && npx vitest run` passes
- HeatmapCanvas.tsx no longer contains `v * v`
- Visual inspection confirms sharper peaks with functional beamforming
</verification>

<success_criteria>
- Frontend renders [0,1] values directly to colormap (no squaring)
- Visual heatmap shows distinct source peaks with sidelobe suppression
- Human verification confirms acceptable visual quality
</success_criteria>

<output>
After completion, create `.planning/phases/19-functional-beamforming-visualization/19-02-SUMMARY.md`
</output>
