---
phase: 21
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/rpi-edge/README.md
  - apps/rpi-edge/requirements.txt
  - apps/rpi-edge/pyproject.toml
  - apps/rpi-edge/skyfort_edge/__init__.py
  - apps/rpi-edge/tests/__init__.py
  - apps/rpi-edge/tests/conftest.py
  - apps/rpi-edge/tests/test_preprocess_parity.py
  - apps/rpi-edge/tests/test_preprocess_drift.py
  - apps/rpi-edge/tests/test_onnx_conversion_sanity.py
  - apps/rpi-edge/tests/test_hysteresis.py
  - apps/rpi-edge/tests/test_gpio_sigterm.py
  - apps/rpi-edge/tests/test_audio_alarm_degrades.py
  - apps/rpi-edge/tests/test_detection_log.py
  - apps/rpi-edge/tests/test_config_merge.py
  - apps/rpi-edge/tests/test_http_endpoints.py
  - apps/rpi-edge/tests/test_resample_48_to_32.py
  - apps/rpi-edge/tests/test_e2e_golden_audio.py
  - apps/rpi-edge/tests/test_inference_latency_host.py
  - apps/rpi-edge/tests/fixtures/.gitkeep
  - apps/rpi-edge/MODEL_HEAD_NOTES.md
autonomous: true
requirements: []
decisions_addressed: []
must_haves:
  truths:
    - "Wave 0 pytest stubs from 21-VALIDATION.md exist and collect"
    - "apps/rpi-edge/ tree exists with skyfort_edge package + tests dir"
    - "models/efficientat_mn10_v6.pt head shape (num_classes) recorded in MODEL_HEAD_NOTES.md"
    - "Pinned dependency manifest exists matching 21-RESEARCH.md versions"
  artifacts:
    - path: "apps/rpi-edge/tests/conftest.py"
      provides: "Shared fixtures: golden_drone_wav, mock_gpio_factory, tmp_config_dir, tmp_jsonl_log"
    - path: "apps/rpi-edge/requirements.txt"
      provides: "Pi-side pinned deps (onnxruntime==1.18.1, gpiozero==2.0.1, etc.)"
    - path: "apps/rpi-edge/MODEL_HEAD_NOTES.md"
      provides: "Recorded num_classes + head shape from .pt inspection"
  key_links:
    - from: "apps/rpi-edge/tests/"
      to: "pytest collection"
      via: "pyproject.toml [tool.pytest.ini_options] testpaths"
      pattern: "testpaths.*rpi-edge"
---

<objective>
Bootstrap the apps/rpi-edge/ tree and install all Wave 0 RED test stubs from 21-VALIDATION.md so subsequent waves have a feedback substrate.

Purpose: Nyquist compliance — every later task can reference an existing failing test. Also inspects models/efficientat_mn10_v6.pt to record the actual num_classes + head shape so Plan 03 (ONNX conversion) and Plan 05 (inference) don't guess.

Output: apps/rpi-edge/ skeleton with skyfort_edge package, 13 RED pytest stubs, pinned requirements.txt, conftest.py with shared fixtures, and MODEL_HEAD_NOTES.md with checkpoint introspection results.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-build-raspberry-pi-4-edge-drone-detection-app-using-efficien/21-CONTEXT.md
@.planning/phases/21-build-raspberry-pi-4-edge-drone-detection-app-using-efficien/21-RESEARCH.md
@.planning/phases/21-build-raspberry-pi-4-edge-drone-detection-app-using-efficien/21-VALIDATION.md
@src/acoustic/classification/efficientat/preprocess.py
@src/acoustic/classification/efficientat/model.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create apps/rpi-edge/ skeleton + pinned dependency manifests</name>
  <files>apps/rpi-edge/README.md, apps/rpi-edge/requirements.txt, apps/rpi-edge/pyproject.toml, apps/rpi-edge/skyfort_edge/__init__.py, apps/rpi-edge/tests/__init__.py, apps/rpi-edge/tests/fixtures/.gitkeep</files>
  <read_first>
    - .planning/phases/21-build-raspberry-pi-4-edge-drone-detection-app-using-efficien/21-RESEARCH.md (sections "Standard Stack" and "Recommended Project Structure")
    - .planning/phases/21-build-raspberry-pi-4-edge-drone-detection-app-using-efficien/21-CONTEXT.md (D-25, D-27, D-28)
  </read_first>
  <action>
Create the directory tree under `apps/rpi-edge/`:
- `skyfort_edge/__init__.py` (empty package marker, single line: `"""Sky Fort edge drone-detection app for Raspberry Pi 4."""`)
- `tests/__init__.py` (empty)
- `tests/fixtures/.gitkeep` (empty)
- `README.md` — STUB only with header `# Sky Fort Edge (Raspberry Pi 4)` and one-line description: "Standalone edge drone-detection app. Detailed docs land in Plan 21-08." Do NOT write install instructions yet — Plan 08 owns that.

Create `apps/rpi-edge/requirements.txt` with EXACTLY these pinned versions (copy verbatim from 21-RESEARCH.md "Installation (Pi)" block):
```
numpy==1.26.4
scipy==1.14.1
sounddevice==0.5.1
PyYAML==6.0.2
onnxruntime==1.18.1
gpiozero==2.0.1
lgpio==0.2.2.0
```

Create `apps/rpi-edge/pyproject.toml`:
```toml
[project]
name = "skyfort-edge"
version = "0.1.0"
description = "Sky Fort edge drone-detection app for Raspberry Pi 4"
requires-python = ">=3.11,<3.12"
dependencies = [
    "numpy==1.26.4",
    "scipy==1.14.1",
    "sounddevice==0.5.1",
    "PyYAML==6.0.2",
    "onnxruntime==1.18.1",
    "gpiozero==2.0.1",
    "lgpio==0.2.2.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.0", "pytest-asyncio>=0.24"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
```
  </action>
  <verify>
    <automated>test -f apps/rpi-edge/requirements.txt && test -f apps/rpi-edge/pyproject.toml && grep -q "onnxruntime==1.18.1" apps/rpi-edge/requirements.txt && grep -q "gpiozero==2.0.1" apps/rpi-edge/requirements.txt && test -d apps/rpi-edge/skyfort_edge && test -d apps/rpi-edge/tests/fixtures</automated>
  </verify>
  <acceptance_criteria>
    - `apps/rpi-edge/requirements.txt` contains exact line `onnxruntime==1.18.1`
    - `apps/rpi-edge/requirements.txt` contains exact line `gpiozero==2.0.1`
    - `apps/rpi-edge/pyproject.toml` contains `requires-python = ">=3.11,<3.12"`
    - `apps/rpi-edge/skyfort_edge/__init__.py` exists
    - `apps/rpi-edge/tests/__init__.py` exists
  </acceptance_criteria>
  <done>Skeleton tree exists, deps pinned to research-locked versions, pytest can discover the testpaths.</done>
</task>

<task type="auto">
  <name>Task 2: Inspect efficientat_mn10_v6.pt + record head shape + create golden audio fixture</name>
  <files>apps/rpi-edge/MODEL_HEAD_NOTES.md, apps/rpi-edge/tests/fixtures/golden_drone_1s_48k.wav, apps/rpi-edge/tests/fixtures/golden_silence_1s_48k.wav</files>
  <read_first>
    - models/efficientat_mn10_v6.pt (binary — load via torch on host)
    - src/acoustic/classification/efficientat/model.py (MN class, head construction)
    - src/acoustic/classification/efficientat/config.py (training-side constants)
  </read_first>
  <action>
Run a one-off Python introspection on the host (Claude executes via Bash + python -c, no test framework needed). Load the checkpoint with torch.load, extract:
1. `num_classes` from the classifier head weight shape (e.g. `state_dict['classifier.weight'].shape[0]`)
2. The full head shape tuple
3. Total parameter count
4. List of top-level state_dict keys

Write `apps/rpi-edge/MODEL_HEAD_NOTES.md`:
```markdown
# efficientat_mn10_v6.pt — Head Shape & Class Count

Inspected: <YYYY-MM-DD>
Source: models/efficientat_mn10_v6.pt

## Head Shape
- num_classes: <N>
- classifier.weight.shape: <tuple>
- total_params: <int>

## Top-level state_dict keys
<list>

## Notes for downstream plans
- Plan 21-03 (ONNX conversion): export shape (1, 1, 128, T_FRAMES) → (1, num_classes)
- Plan 21-05 (inference): output tensor has shape (batch, num_classes)
- If num_classes == 1: binary sigmoid; if > 1: multi-class softmax / per-class threshold (D-11 per_class_thresholds)
```

Create golden audio fixtures for the parity / e2e tests:
- `apps/rpi-edge/tests/fixtures/golden_drone_1s_48k.wav` — 1.0 s, mono, 48 kHz, int16. Synthesize a 200 Hz + 400 Hz + 800 Hz sine mixture (drone-like harmonic stack) at 0.3 amplitude using numpy + soundfile or scipy.io.wavfile. Deterministic seed.
- `apps/rpi-edge/tests/fixtures/golden_silence_1s_48k.wav` — 1.0 s, mono, 48 kHz, int16, all zeros.

Use a single inline script (not a committed generator) to produce them. Commit the .wav files directly.
  </action>
  <verify>
    <automated>test -f apps/rpi-edge/MODEL_HEAD_NOTES.md && grep -q "num_classes:" apps/rpi-edge/MODEL_HEAD_NOTES.md && test -f apps/rpi-edge/tests/fixtures/golden_drone_1s_48k.wav && test -f apps/rpi-edge/tests/fixtures/golden_silence_1s_48k.wav && python -c "import soundfile as sf; d, sr = sf.read('apps/rpi-edge/tests/fixtures/golden_drone_1s_48k.wav'); assert sr == 48000 and len(d) == 48000, f'bad shape: sr={sr} len={len(d)}'"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/rpi-edge/MODEL_HEAD_NOTES.md` contains a `num_classes:` line with an integer value
    - `apps/rpi-edge/MODEL_HEAD_NOTES.md` contains `classifier.weight.shape:` line
    - Golden drone WAV is exactly 48000 samples at 48 kHz mono
    - Golden silence WAV is exactly 48000 samples, all zeros
  </acceptance_criteria>
  <done>Checkpoint head shape recorded for downstream plans; deterministic golden WAV fixtures committed for parity + e2e tests.</done>
</task>

<task type="auto">
  <name>Task 3: Install all 13 Wave 0 RED test stubs + conftest.py from 21-VALIDATION.md</name>
  <files>apps/rpi-edge/tests/conftest.py, apps/rpi-edge/tests/test_preprocess_parity.py, apps/rpi-edge/tests/test_preprocess_drift.py, apps/rpi-edge/tests/test_onnx_conversion_sanity.py, apps/rpi-edge/tests/test_hysteresis.py, apps/rpi-edge/tests/test_gpio_sigterm.py, apps/rpi-edge/tests/test_audio_alarm_degrades.py, apps/rpi-edge/tests/test_detection_log.py, apps/rpi-edge/tests/test_config_merge.py, apps/rpi-edge/tests/test_http_endpoints.py, apps/rpi-edge/tests/test_resample_48_to_32.py, apps/rpi-edge/tests/test_e2e_golden_audio.py, apps/rpi-edge/tests/test_inference_latency_host.py</files>
  <read_first>
    - .planning/phases/21-build-raspberry-pi-4-edge-drone-detection-app-using-efficien/21-VALIDATION.md (Wave 0 Requirements section — exact bullet list)
    - apps/rpi-edge/MODEL_HEAD_NOTES.md (from Task 2 — for hysteresis test class count)
  </read_first>
  <action>
Create `apps/rpi-edge/tests/conftest.py` with these exact fixtures:
```python
import pytest
from pathlib import Path
import numpy as np
import soundfile as sf

FIXTURES = Path(__file__).parent / "fixtures"

@pytest.fixture
def golden_drone_wav():
    audio, sr = sf.read(FIXTURES / "golden_drone_1s_48k.wav")
    return audio.astype(np.float32), sr

@pytest.fixture
def golden_silence_wav():
    audio, sr = sf.read(FIXTURES / "golden_silence_1s_48k.wav")
    return audio.astype(np.float32), sr

@pytest.fixture
def mock_gpio_factory():
    from gpiozero import Device
    from gpiozero.pins.mock import MockFactory
    prev = Device.pin_factory
    Device.pin_factory = MockFactory()
    yield Device.pin_factory
    Device.pin_factory = prev

@pytest.fixture
def tmp_config_dir(tmp_path):
    d = tmp_path / "config"
    d.mkdir()
    return d

@pytest.fixture
def tmp_jsonl_log(tmp_path):
    return tmp_path / "detections.jsonl"
```

Create EACH of the 13 test stub files. Each stub must:
1. Be a runnable pytest module (importable, collects without ImportError)
2. Contain at least one `def test_*` that uses `pytest.fail("not implemented — Wave 1+ task X owns this")` OR `pytest.xfail(reason="RED stub")` so it shows up as xfail/red
3. Include a docstring at the top with the D-XX decision IDs it covers
4. NOT import modules that don't exist yet — use `pytest.importorskip("skyfort_edge.module_name")` or wrap imports in try/except + skip

Use these EXACT test names (matching 21-VALIDATION.md):

`test_preprocess_parity.py` (covers D-04):
- `test_vendored_preprocess_matches_training_reference_within_atol_1e_5`

`test_preprocess_drift.py` (covers D-04):
- `test_preprocess_py_byte_identical_to_training_source`
- `test_mel_banks_pt_byte_identical_to_training_source`

`test_onnx_conversion_sanity.py` (covers D-06, D-07, D-08):
- `test_fp32_onnx_top1_agreement_ge_99pct`
- `test_int8_onnx_top1_agreement_ge_97pct`
- `test_mean_logit_delta_below_threshold`

`test_hysteresis.py` (covers D-12, D-14):
- `test_rising_edge_latches_after_confirm_hits`
- `test_min_on_seconds_held_after_last_positive`
- `test_release_after_release_hits_below_exit_threshold`

`test_gpio_sigterm.py` (covers D-15):
- `test_sigterm_drives_led_pin_low`
- `test_sigterm_releases_gpio_factory`

`test_audio_alarm_degrades.py` (covers D-19):
- `test_missing_audio_device_logs_warning_and_continues`
- `test_playback_failure_does_not_crash_pipeline`

`test_detection_log.py` (covers D-20, D-21, D-22):
- `test_one_jsonl_record_per_latch`
- `test_required_fields_present`
- `test_size_based_rotation`
- `test_cannot_be_disabled_via_config`

`test_config_merge.py` (covers D-09, D-10, D-11):
- `test_yaml_loads_with_safe_load_only`
- `test_cli_overrides_yaml`
- `test_unknown_top_level_key_rejected`
- `test_default_paths_match_research`

`test_http_endpoints.py` (covers D-24):
- `test_health_returns_200_json`
- `test_status_returns_200_json`
- `test_binds_only_to_127_0_0_1`
- `test_unknown_path_returns_404`

`test_resample_48_to_32.py` (covers D-02):
- `test_resample_poly_2_3_correctness`
- `test_resample_latency_under_50ms_for_1s_window`

`test_e2e_golden_audio.py` (covers D-03, D-12, D-14, D-20):
- `test_golden_drone_wav_produces_latched_detection`
- `test_golden_silence_wav_produces_no_detection`

`test_inference_latency_host.py` (covers D-05, D-07):
- `test_int8_onnx_inference_under_150ms`

Each test must use `pytest.fail` or `pytest.xfail` with a clear "Wave N task M owns this" message. They must collect and run without crashing. They will all be marked xfail until later plans implement them.
  </action>
  <verify>
    <automated>cd apps/rpi-edge && python -m pytest tests/ --collect-only -q 2>&1 | tail -20 && python -m pytest tests/ --collect-only -q 2>&1 | grep -c "test_" | awk '{ if ($1 < 25) { print "FAIL: only " $1 " tests collected, expected >=25"; exit 1 } else { print "OK: " $1 " tests collected" } }'</automated>
  </verify>
  <acceptance_criteria>
    - `pytest apps/rpi-edge/tests --collect-only` exits 0 (no ImportError, no SyntaxError)
    - At least 25 test items are collected across the 13 files
    - `apps/rpi-edge/tests/conftest.py` defines fixtures: `golden_drone_wav`, `golden_silence_wav`, `mock_gpio_factory`, `tmp_config_dir`, `tmp_jsonl_log` (verify with grep)
    - All 13 test files exist
    - Running `pytest apps/rpi-edge/tests -q --tb=no` shows xfail/fail count > 0 (none green yet — that is correct for Wave 0 RED stubs)
  </acceptance_criteria>
  <done>All 13 Wave 0 stubs collect; fixtures available; pytest can run apps/rpi-edge/tests without import errors. Phase 21 has a feedback substrate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host filesystem → repo | Wave 0 only writes inside apps/rpi-edge/ — no other paths touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-08 | Tampering | Wave 0 stub tests pass without implementation | mitigate | Every stub uses pytest.fail/xfail with "not implemented" message; CI must show non-zero failing count after Wave 0 |
| T-21-05 | Tampering | Model file tampering (precursor) | accept | Checksum recording deferred to Plan 21-03 (conversion script writes checksum); Wave 0 only records head shape |
</threat_model>

<verification>
- `pytest apps/rpi-edge/tests --collect-only` exits 0
- All 13 Wave 0 stubs from 21-VALIDATION.md exist and collect
- requirements.txt contains all 7 pinned versions from 21-RESEARCH.md
- MODEL_HEAD_NOTES.md exists with num_classes recorded
</verification>

<success_criteria>
Phase 21 has a working test substrate. Subsequent waves can run `pytest apps/rpi-edge/tests -x -q` after every commit (Nyquist sampling) and watch RED stubs flip GREEN.
</success_criteria>

<output>
Create `.planning/phases/21-build-raspberry-pi-4-edge-drone-detection-app-using-efficien/21-01-SUMMARY.md` after completion.
</output>
</content>
</invoke>