---
context: default
phase: ad-hoc-debug (no active phase — live pipeline troubleshooting)
task: 1
total_tasks: 4
status: paused
last_updated: 2026-04-07T11:32:28.488Z
---

<current_state>
Ad-hoc debug session: user reported "pipeline keeps checking the same sample". Investigation showed the pipeline loop/ring buffer/CNN cadence are all working — the CNN classifier itself is outputting a near-constant 0.5143 ± 0.0002 on every audio segment. Session paused at 90% context before implementing the requested computer-mic toggle. No code changes left on disk; one attempted edit to `src/acoustic/classification/worker.py` was fully reverted.

This is NOT phase work — phase 20.1 (noise corpora acquisition) is the nominal active phase but unrelated to this debugging detour.
</current_state>

<completed_work>

Investigation / diagnosis:
- Verified backend + frontend are running (uvicorn PID 5985 on :8000, vite PID 98258 on :5173). Health endpoint returns ok, UMA16v2 detected, 0 overflows.
- Confirmed via `/api/pipeline/status` and `/api/pipeline/log` that `drone_probability` is glued at 0.5143 ± 0.0002 over 5+ seconds of real mic audio. Detection log has 200 entries all formatted `51.4%`.
- Walked through the plumbing: `pipeline.py` loop pushes every `_cnn_interval=0.2s`, window=0.5s, overlap=60% (by design), dedup-by-timestamp is working. Ring buffer fresh frames confirmed.
- Session state: `model_path=models/research_cnn_trained.pt`, `confidence=0.08` (workaround-low), `gain=200`, `positive_detections=1`, `state=DRONE_CONFIRMED`. 0.514 trivially exceeds 0.08 so state is permanently confirmed.
- File sizes: `research_cnn_trained.pt` = **449 KB** vs EfficientAT variants = **~17 MB**. Suspicious — possible stub/under-trained.
- Inspected `src/acoustic/classification/preprocessing.py`:
  - `RawAudioPreprocessor.process()` (line 195–214): resample → multiply by `input_gain` → RMS normalize to 0.1. The RMS normalize **overwrites** the input_gain multiplication — docstring at line 151-153 confirms it's a no-op.
  - `ResearchPreprocessor.process()` (line 261-307): no input_gain applied at all. Uses `ref=max` dB normalization + clip to [0,1], so absolute input level is structurally irrelevant.
  - `main.py:312-319` picks `ResearchPreprocessor` whenever `cnn_model_type` isn't EfficientAT/mn10/mn05. So for research_cnn, the `gain=200` field in the session is a pure no-op.
- Conclusion: flat 0.514 is **not a preprocessing problem**. Almost certainly the model weights themselves (untrained / mis-loaded / stub).
</completed_work>

<remaining_work>

Tasks in TaskList (IDs match internal task IDs):

- **Task 1 — Add computer-mic audio source toggle** (not started; requested after investigation). Backend: new `ComputerMicCapture` class in `src/acoustic/audio/capture.py` that opens the default input device and tiles mono → 16 channels so the ring buffer shape `(ring_chunks, chunk_samples, 16)` is preserved. `POST /api/pipeline/audio-source {source: "uma16"|"computer"}` endpoint in `src/acoustic/api/pipeline_routes.py` that tears down the current capture and calls `pipeline.restart(new_ring)`. Gate `_device_lifecycle_task` in `src/acoustic/main.py:142` to also skip when a "computer" override is active (mirror the existing `settings.audio_source == "simulated"` check). Frontend: add segmented control at the top of `web/src/components/pipeline/PipelinePanel.tsx` and a mutation hook in `web/src/hooks/usePipeline.ts`.

- **Task 2 — Log raw per-segment prob in CNN worker** (not started; partial edit reverted). Add `_raw_probs_debug: deque[tuple[float, float, float]]` (timestamp, raw_prob, aggregated_prob) to `CNNWorker.__init__` in `src/acoustic/classification/worker.py`. Populate in `_loop` right before aggregation (around line 157, between `self._segment_probs.append(prob)` and the aggregator call). Add `get_raw_probs()` method. Add `GET /api/pipeline/debug/raw-probs` endpoint. Goal: confirm whether the 0.514 flatness is already present in the raw classifier output or introduced by `WeightedAggregator`.

- **Task 4 — Restart both servers** (not started). After code changes, kill uvicorn PID 5985 and vite PID 98258, then restart fresh using README commands. The current backend cannot pick up code changes without a restart.
</remaining_work>

<decisions_made>

- **Paused before implementing computer-mic toggle** due to context at 90% — the toggle requires new backend class + endpoint + lifecycle gating + frontend hook + UI + server restart, too much for the remaining budget.
- **Computer-mic capture will tile mono → 16 channels** so the ring buffer shape stays `(ring_chunks, chunk_samples, 16)` and the pipeline's downstream code (which averages to mono via `chunk.mean(axis=1)` in `pipeline.py:366` for the CNN path) works unchanged. Beamforming output becomes meaningless on a computer mic but that's not what we're debugging.
- **Diagnosis conclusion**: the flat 0.5143 is a model-weights issue, not a pipeline / preprocessing / gain issue. Leading hypothesis is that `research_cnn_trained.pt` is under-trained or a stub (449 KB is tiny).
</decisions_made>

<blockers>
- **Live backend logs not tailable**: the running uvicorn (PID 5985) was started outside this session, so its stdout can't be tailed. Any `logger.info` added to the code won't be visible until the backend is restarted. Mitigation: expose debug state via a `/api/pipeline/debug/*` endpoint instead of (or in addition to) logging.
- **Pipeline.activate_model loader bug (noted, not touched)**: `src/acoustic/api/pipeline_routes.py:51-71` always loads the activated model as `ResearchCNN`, even if the path is an EfficientAT file. Not the bug we're chasing, but worth flagging for a future cleanup.
</blockers>

## Required Reading (in order)
1. `src/acoustic/pipeline.py` — understand the loop + CNN push cadence + dedup-by-timestamp
2. `src/acoustic/classification/worker.py` — where task 2 patches go (line ~157)
3. `src/acoustic/audio/capture.py` — where the new `ComputerMicCapture` class goes
4. `src/acoustic/main.py:124-215` — `_device_lifecycle_task` and `_create_hardware_capture`, which need to be gated for computer-mic mode
5. `src/acoustic/api/pipeline_routes.py` — where the new audio-source endpoint goes
6. `web/src/components/pipeline/PipelinePanel.tsx` + `web/src/hooks/usePipeline.ts` — where the UI toggle + hook go
7. `src/acoustic/classification/preprocessing.py:140-230` — already read; confirms `input_gain` is a no-op and `ResearchPreprocessor` ignores gain entirely

## Critical Anti-Patterns (do NOT repeat these)
- ANTI-PATTERN: Treating "same formatted probability" as "same audio sample". The detection log formats probs to one decimal percent, so a classifier output that wiggles in the 4th decimal looks like repeated identical entries. Always sample `/api/pipeline/status` with full precision before concluding the pipeline is frozen.
- ANTI-PATTERN: Debugging preprocessing as the cause of flat classifier outputs before verifying the model file is legitimate. `ResearchPreprocessor` uses `ref=max` dB normalization, which structurally removes absolute level. Gain/saturation hypotheses are dead on arrival.

## Infrastructure State
- Backend: `uvicorn acoustic.main:app --host 0.0.0.0 --port 8000` running as PID 5985 since 5:42 PM (~98 % CPU, 861+ CPU-min accumulated). `AUDIO_SOURCE=simulated` was set on the original launch but UMA16v2 is detected and in use.
- Frontend: `vite` running as PID 98258 on port 5173 since 5:25 PM.
- Both were started outside this Claude session → their stdout/logs are not tailable from here. Any debug logging added will only be visible after a restart.
- Detection session currently running in-backend: `model_path=models/research_cnn_trained.pt`, `confidence=0.08`, `positive_detections=1`, `gain=200`, `window=0.5s`, `interval=0.2s`, `state=DRONE_CONFIRMED`.
- Git: on `main`, multiple modified and untracked files from phase 20.1 work (see `HANDOFF.json` `uncommitted_files` list) — unrelated to this debug session and should NOT be included in the pause-work commit.

<context>
User's original complaint was "keep getting the same prob on pipeline like it keep checking same sample". I initially thought this was a pipeline loop bug but verified it's a flat classifier output. User then asked me to (a) add a computer-mic toggle to test the model against regular mic audio, (b) add raw-prob logging to see if the aggregator is the smoother, (c) inspect `RawAudioPreprocessor`. I finished (c), started (b) with a single worker.py edit which I reverted when context hit 89 %, and never started (a). Next session should open with a one-minute reality check: swap to `models/efficientat_mn10_v6.pt` via the Pipeline tab and watch `/api/pipeline/status drone_probability` over 5 seconds. If EfficientAT produces varying probs, `research_cnn_trained.pt` is simply broken and the rest of this workplan may be unnecessary.
</context>

<next_action>
1. Swap model to `models/efficientat_mn10_v6.pt` via the Pipeline tab (stop current session, start new one with that path). Sample `/api/pipeline/status` 5 times over 5 seconds. If `drone_probability` varies meaningfully → `research_cnn_trained.pt` is the bad actor; diagnosis done and user can decide whether tasks 1–2 are still wanted.
2. If probs are still flat with a different model → proceed with task 2 (raw-prob debug endpoint) to isolate classifier vs aggregator, then task 1 (computer-mic toggle), then task 4 (restart).
3. When implementing task 1, remember to gate `_device_lifecycle_task` in `src/acoustic/main.py:142` so it doesn't try to reconnect UMA-16 while on computer-mic mode.
</next_action>
